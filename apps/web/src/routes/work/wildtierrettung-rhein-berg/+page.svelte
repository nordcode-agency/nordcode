<script lang="ts">
	import Breadcrumbs from '$lib/modules/common/components/Breadcrumbs.svelte';
	import { page } from '$app/stores';
	import type { Breadcrumb } from '$lib/types/Breadcrumb';
	import type { PageData } from './$types';

	// interface Props {
	// 	data: PageData;
	// }

	// let { data }: Props = $props();

	// const { heading, subheading, cover, name, url, goal, colorPalette, tags } = data;

	// const breadcrumbItems: Breadcrumb[] = [
	// 	{
	// 		name: 'Work',
	// 		url: '/#work'
	// 	},
	// 	{
	// 		name: heading,
	// 		url: $page.url.toString()
	// 	}
	// ];
</script>

<h2>Was haben wir erreicht?</h2>
<p>Ich würde sagen: eine vernünftige Webseite. Aber das ist nur meine Meinung. Was denkst du?</p>

<h2>Umsetzung: Design</h2>
